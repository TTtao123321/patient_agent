`模块一：RAG 核心理论 + 项目落地（AI 后端必考核心）`

1. 理论基础考察

1.1 请完整描述标准 RAG 的全流程架构？RAG 相比直接微调大模型，在医疗场景的核心优势是什么？
1.2 向量数据库（Milvus）的核心原理是什么？为什么检索需要用向量，而不是直接用 MySQL 模糊查询？
1.3 嵌入模型（bge-m3）的作用是什么？向量维度、文本分块（Chunk）对 RAG 召回准确率的影响是什么？
1.4 RAG 流程中，召回（Retrieve） 和 重排序（Rerank） 的区别是什么？医疗知识场景为什么必须加重排序？

2. 深度理论 / 选型考察

2.1 医疗知识库存在专业术语多、文本结构复杂的问题，RAG 分块（Chunking）有哪些优化策略？（递归分块 / 语义分块 / 基于标题分块等）
2.2 向量数据库面临的性能瓶颈有哪些？（召回速度、内存占用、数据量扩容）Milvus 如何解决亿级向量检索问题？
2.3 RAG 常见失效场景（召回无关、召回不全、模型幻觉）的理论解决方案分别是什么？
2.4 混合检索（关键词稀疏检索 + 向量稠密检索）的原理是什么？为什么你的医疗报告解读场景适合用混合检索？

3. 项目落地实战考察（结合你的患者 Agent 项目）

3.1 你在项目中用 LangChain+Milvus+bge-m3 搭建 RAG，医疗知识库的构建流程是怎样的？原始文档如何处理成向量数据？
3.2 你如何解决医疗知识召回不精准的问题？（比如症状相似但病因不同的情况）
3.3 项目中 RAG 是离线入库还是实时入库？向量数据的更新、删除、同步机制如何设计？
3.4 你提到做了 Milvus 降级容错，请问降级策略是什么？向量库不可用时，如何保证医疗咨询服务不宕机？

`模块二：智能 Agent 核心理论 + 项目落地`

1. 理论基础考察

1.1 请定义 AI Agent 的核心架构？（大脑 LLM、感知、记忆、工具、行动五大组件）
1.2 ReAct 原理是什么？它是如何结合「推理（Reasoning）」和「行动（Action）」的？
1.3 思维链（CoT）和 自我一致性（Self-Consistency）的理论作用？为什么医疗推理必须用这两种策略？
1.4 Tool Calling（工具调用）的标准流程是什么？LLM 是如何决定调用哪个工具、传入什么参数的？

2. 深度理论 / 选型考察

2.1 原生 Prompt 对话、函数调用、智能 Agent 三者的核心区别？医疗场景为什么最终选择 Agent 架构？
2.2 Agent 执行失败的常见原因（工具调用失败、参数错误、推理死循环）有哪些理论解决方案？
2.3 LLM 作为 Agent 控制器，存在哪些天然缺陷？（慢、幻觉、逻辑错误）后端如何通过架构弥补？
2.4 结构化输出（如 Pydantic Schema）在 Agent 中的核心价值？为什么医疗场景强制要求结构化输出？

3. 项目落地实战考察（结合你的患者 Agent 项目）

3.1 你融合 ReAct + CoT + 自我一致性做医疗推理，具体在代码中是如何实现的？Prompt 是如何设计的？
3.2 你的 Agent 依赖医疗工具调用，工具调用失败 / 超时 / 返回异常数据时，后端做了哪些容错处理？
3.3 你用 GPT-4o 做推理控制器，为什么选择它而不是本地 Ollama+Qwen？两者在 Agent 推理上的优劣？
3.4 项目中如何避免 Agent 出现医疗错误推理？如何通过 RAG + 规则约束双重保障回答安全性？

`模块三：多 Agent 架构 理论与工程实现（AI 后端高阶考点）`

1. 理论基础考察

1.1 单 Agent 和 多 Agent 架构的核心区别？多 Agent 的协作模式有哪些？（路由式、中心化协作、分布式协作）
1.2 你提到的 MCP Server（模块化通信协议）理论定义是什么？多模块 Agent 为什么需要 MCP 解耦？
1.3 Router Agent（路由 Agent）的核心设计模式是什么？意图识别的两种方案（LLM 识别 / 规则识别）优劣对比？

2. 深度理论 / 选型考察

2.1 多 Agent 架构的分布式事务 / 一致性问题是什么？如何避免多个 Agent 之间的逻辑冲突？
2.2 模块化拆分 Agent 的原则是什么？（按业务域 / 按能力 / 按数据权限）你的医疗项目为什么拆分为症状 / 报告 / 知识 Agent？
2.3 多 Agent 系统的可扩展性设计要点？如何快速新增一个医疗 Agent 模块（如用药咨询）？

3. 项目落地实战考察

3.1 你设计的「LLM 意图识别 + 关键词回退」路由策略，理论依据是什么？解决了纯 LLM 路由的哪些问题？
3.2 多 Agent 之间的会话数据如何共享？如何保证记忆数据在不同 Agent 间的一致性？
3.3 MCP Server 标准化接口如何定义？Java 业务后端如何无感调用不同的 Agent 模块？

`模块四：AI Agent 记忆系统 理论与后端实现（核心加分项）`

1. 理论基础考察

1.1 AI Agent 记忆的标准分类：短期记忆、长期记忆、语义记忆、用户记忆 分别是什么？
1.2 记忆抽取（关键信息提取）的两种方案：规则抽取 vs LLM 抽取 的理论对比？
1.3 记忆存储的分层架构设计原则？为什么不能用单一数据库存储所有类型的记忆？

2. 深度理论 / 选型考察

2.1 会话记忆的过期策略、淘汰机制、压缩机制的理论方案？如何解决长对话记忆冗余问题？
2.2 向量记忆（语义记忆）和 结构化记忆（MySQL）的配合原理？如何实现「精准检索 + 语义理解」双能力？

3. 项目落地实战考察

3.1 你设计的 Redis（短期）+ MySQL（长期）+ 向量（语义） 三层记忆，理论上的分工边界是什么？
3.2 用户画像、关键事件自动抽取用的是 LLM 还是规则？抽取的医疗核心字段有哪些？如何保证抽取准确率？
3.3 记忆 API 如何对接 Java 后端和 AI 服务？如何保证多服务调用时的记忆数据不丢失？

`模块五：AI 应用后端 高并发 / 流式 / 异步 工程化理论（生产级必考）`

1. 理论基础考察

1.1 AI 服务（LLM/RAG）相比传统后端服务，性能瓶颈在哪里？（慢响应、高算力、不可控耗时）
1.2 流式输出（StreamingResponse/SSE）的核心原理？为什么 AI 对话必须用流式，而不是普通 HTTP 返回？
1.3 异步消息队列（RabbitMQ）在 AI 后端的核心应用场景？同步 / 异步调用的理论选型标准？

2. 深度理论 / 选型考察

2.1 AI 后端高并发架构的核心设计：限流、熔断、降级、排队 分别解决什么问题？
2.2 跨语言（Java→Python）AI 服务调用的常见问题？（超时、序列化、链路追踪）理论解决方案？
2.3 SSE 长连接 vs WebSocket 在 AI 流式对话中的优劣？为什么你的项目选择 SSE？

3. 项目落地实战考察

3.1 你实现的 FastAPI StreamingResponse + Java SseEmitter 跨服务流式，理论上如何保证数据流不中断、不乱序？
3.2 RabbitMQ 处理 AI 异步任务时，如何解决消息堆积、消息丢失、重复消费三大问题？
3.3 高峰期医疗咨询并发高，你做了哪些后端优化？（模型池、队列排队、服务限流）如何保证服务高可用？

`模块六：AI 应用后端 通用理论（医疗场景专属）`

1.1 医疗 AI 应用的合规性对后端开发的要求是什么？（数据隐私、模型输出审核、免责机制）
1.2 Prometheus 监控 AI 后端的核心指标有哪些？（Agent 响应耗时、RAG 召回率、报告解析成功率、模型失败率）
1.3 全链路追踪（RequestId）在 Java→Python 跨服务 AI 架构中的作用？如何快速排查线上问题？